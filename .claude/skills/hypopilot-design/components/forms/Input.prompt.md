Champ de formulaire. Montants et taux : `mono` + `prefix="CHF"` ou `suffix="%"`.

```jsx
<Input label="Montant de l'hypothèque" prefix="CHF" mono placeholder="650 000" hint="Le montant restant dû." />
```
